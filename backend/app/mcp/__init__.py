"""Клиент протокола передачи контекста моделям (Т-503).

Подключение внешних инструментов по HTTP к явному адресу (решение 1
дизайн-ревью Т-503 / пункт 8 ADR-21): реестр серверов ведёт
администратор, секреты шифруются механизмом ключей провайдеров,
инструменты внешних серверов входят в единый реестр инструментов
агентного модуля с меткой источника. Дополнение ``orqion[mcp]`` не
входит в зависимости по умолчанию: без него модуль честно сообщает о
недоступности (паттерн деградации Т-444/Т-502/Т-505), а не падает при
импорте.
"""
