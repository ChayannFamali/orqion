"""DLP-детекторы — опциональная страховка (ADR-13).

Детекторы персональных данных и секретов подключаются как плагины
и по умолчанию выключены.

ОГРАНИЧЕНИЯ (ADR-13, arch.md — документация обязана называть прямо):
- Не выявляют смысловую конфиденциальность
- Не выявляют имена без сопутствующих документов
- Не выявляют содержимое неразобранных вложений
- Не выявляют целенаправленный обход
- Страховка от случайной вставки, не средство защиты от умысла

При срабатывании фиксируются: факт, пользователь, модель, типы
сработавших детекторов и хеш запроса. Содержимое не логируется.

Механизм регистрации — простой Python-вызов (register_detector),
не setuptools entry_points. При появлении встроенных детекторов
может быть расширен до entry_points.

Детектор сканирует весь messages list, отправляемый провайдеру,
включая историю диалога. При длинном диалоге одно и то же содержимое
может срабатывать повторно — это осознанное поведение (каждый запрос
фактически повторно отправляет контент наружу). Дедупликация логов —
будущая задача.
"""

from __future__ import annotations

from app.detectors.protocol import Detector, DetectorResult
from app.detectors.registry import clear_detectors, get_detectors, register_detector

__all__ = [
    "Detector",
    "DetectorResult",
    "clear_detectors",
    "get_detectors",
    "register_detector",
]
