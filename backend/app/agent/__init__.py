"""Агентный модуль (Т-502).

Цикл «модель → инструменты → модель» на LangGraph, синхронное
выполнение в одном запросе (решение 2 пересмотренного дизайн-ревью).
Дополнение ``orqion[agent]`` не входит в зависимости по умолчанию:
без него модуль честно сообщает о недоступности (паттерн деградации
Т-444/Т-505), а не падает при импорте.
"""
